<% conversation.participants.each do |participant| %>
  <% unless participant == current_user %>
    <%= cl_image_tag avatar_image(participant), width: 100, height: 100, crop: :fill, gravity: :face, class: "avatar" %>
  <% end %>
<% end %>
